# Copy to .env.local for local testing (`just start`) against wrangler dev.
THUMBNAIL_WORKER_URL=http://localhost:8787
THUMBNAIL_PUSH_SECRET=changeme
THUMBNAIL_POLL_INTERVAL_MINUTES=60

# CF Access doesn't front localhost — leave blank.
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=
