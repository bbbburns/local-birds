{
  "name": "vega-vireos-cf",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0",
    "@cloudflare/workers-types": "^4",
    "typescript": "^6",
    "vitest": "^4",
    "wrangler": "^4"
  }
}
